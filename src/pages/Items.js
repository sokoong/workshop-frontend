import React from "react";

function Items() {
  // const token = localStorage.getItem("token");
  // const payload = JSON.parse(token.split(".")[1]);
  // const email = payload.email;

  return (
    <div>
      <h1>Welcome</h1>
      <p>This is your Items.</p>
    </div>
  );
}

export default Items;
